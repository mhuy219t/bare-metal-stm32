/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 ******************************************************************************
 * @attention
 *
 * Copyright (c) 2026 STMicroelectronics.
 * All rights reserved.
 *
 * This software is licensed under terms that can be found in the LICENSE file
 * in the root directory of this software component.
 * If no LICENSE file comes with this software, it is provided AS-IS.
 *
 ******************************************************************************
 */

#include <stdint.h>

#define RCC_BASE_ADDRESS		0x40023800UL
#define RCC_CFGR_REG_OFFSET		0x08UL
#define RCC_CFGR_REG_ADDRESS	(RCC_BASE_ADDRESS + RCC_CFGR_REG_OFFSET)

#define RCC_CR_REG_OFFSET		0x00UL
#define RCC_CR_REG_ADDRESS		(RCC_BASE_ADDRESS + RCC_CR_REG_OFFSET)
#define HSEON_BIT_POSITION		16u
#define HSERDY_BIT_POSITION		17u

int main(void)
{
	uint32_t *pRccCrReg = (uint32_t*)RCC_CR_REG_ADDRESS;
	uint32_t *pRccCfgrReg = (uint32_t*)RCC_CFGR_REG_ADDRESS;

	// 1. Enable the HSE clock using HSEON bit in RCC_CR
	*pRccCrReg |= (1 << HSEON_BIT_POSITION);

	// 2. Wait until HSE clock from the external crystal stabilizes
	while (!(*pRccCrReg & (1 << HSERDY_BIT_POSITION)));

	// 3. Switch the system clock to HSE (RCC_CFGR)
	*pRccCfgrReg |= (1 << 0);

	// 4. Do MCO1 settings to measure it
    /* Loop forever */
	for(;;);
}
